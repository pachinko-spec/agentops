# agentops-watch が読む監視対象プロジェクトの雛形です。
# secret は書かず、通知先は環境変数で渡します。
# 実プロジェクトは原則 ~/dev 配下を明示的に追加します。dotfiles は明示的に対象化しない限り含めません。

projects:
  - name: agentops
    path: .
    default_branch: main
    stuck_run_hours: 2
    max_behind_commits: 10
    max_ahead_commits: 50
