# Codex cross-review: task 01 P0-02 (tool 実行層停止条件)

> 親 plan: `2026-04-28-design-review-p0-p1`  
> task: `.agentops/tasks/01-p0-02-tool-stop-conditions.md`  
> Reviewer: Codex (`scripts/agentops delegate --to codex --role review_frontier --effort high`)  
> Run: `.agentops/runs/20260428t0956110000-p0-02/`  
> 主 orchestrator (Claude Opus 4.7) 判断: 採用、P2/P3 全件反映済み（1 周で完了）

## Codex 所見

- **P0**: なし
- **P1**: なし
- **P2**: 2 件
  1. docs/12 の `docs/03` 参照がインラインコードで、task 完了条件「相互リンク」と整合させるなら実リンク化推奨
  2. `defaults.stop_conditions.tool_layer` と `task_template.stop_conditions` が別形状。将来の監視 CLI 実装時の merge / 優先順位を明記推奨
- **P3**: 1 件
  - `cost_cap_usd_per_session: 20` は「支出許可」ではなく「停止上限」と明記推奨（誤解防止）

## 確認観点への回答（Codex）

- 2 階層構造は運用しやすい。プロセス層と tool 実行層の分離も自然
- 4 閾値は初期値として妥当。`max_tool_calls=200` / `no_progress_steps=10` / `cycle=3` は過剰に細かすぎない。`cost_cap=20` だけは「予算」ではなく停止上限と明記推奨
- `schema_version` は今回上げなくてよい（additive な defaults 追加で、現時点の consumer 実装なし）
- 既存の単一停止条件記述は docs/09 / docs/10 / docs/11 に残るが、component 固有 DbC として許容範囲。今回必ず 2 階層化すべき見落としではない

## 反映方針と差分

| 指摘 | 対応 | 反映先 |
|---|---|---|
| P2-1 | docs/12 の参照を `[DbCと品質ゲート](03-dbc-and-quality-gates.md#停止条件)` に実リンク化 | docs/12-harness-engineering.md L70 |
| P2-2 | `defaults.stop_conditions` と `task_template.stop_conditions` の責務（基底値 vs 個別追加条件）をコメントで明記 | config/harness.yml |
| P3 | `cost_cap_usd_per_session` のコメントを「支出許可ではない、超過時に halt」に変更 | config/harness.yml |

## 採否判断

- 全件反映（軽量修正、運用品質改善）。1 周で完了。
- `docs/09 / 10 / 11` の単一停止条件記述は本 task 範囲外（Codex も「2 階層化すべき見落としではない」と判定）。次の DbC 集約 task (P1-05) で必要なら再評価。

## 残タスク

なし（P0/P1 = 0、P2/P3 全件反映済み、CI green、main 同期可）。auto-merge 許諾条件を満たす。
