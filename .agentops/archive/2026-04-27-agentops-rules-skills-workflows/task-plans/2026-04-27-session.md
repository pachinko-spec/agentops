# Task Plan: rules/skills/workflows整理

親plan: `.agentops/plans/current.md`
承認: explicit-approved
状態: done

## 今回セッションの目的

承認済みplanを実装し、完了したtaskとplanをplan単位のarchiveへ移せる状態にする。

## 実行順

1. `.agentops` のplan/task/task-plan/archive運用を整える。
2. `/ai` を撤去し、Understand-Anything policyを `config/` へ移す。
3. `rules/` を新設する。
4. `skills/` と `workflows/` を拡張する。
5. `README.md`、`docs/`、`config/` に反映する。
6. 検証する。
7. 完了したtaskとplanをarchiveへ移す。

## 変更されうる点

- 作業中に既存docsとの重複が見つかった場合は、DRY原則に従い正本を寄せる。
- 前タスクの結果で後続taskが不要になった場合は、taskを中止扱いでarchiveへ移す。

## 復帰ポイント

最後に完了したtask: all
次に読むtask: なし
停止理由: 完了
