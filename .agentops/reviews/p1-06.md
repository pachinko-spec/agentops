# Codex cross-review: task 07 P1-06 (archive 自動更新 hook + 個別 task archive CLI)

> 親 plan: `2026-04-28-design-review-p0-p1`
> task: `.agentops/tasks/07-p1-06-archive-auto-update.md`
> Reviewer: Codex (`scripts/agentops delegate --to codex --role review_frontier --effort high`)
> Run: `.agentops/runs/20260428t194500-p1-06/`
> 主 orchestrator (Claude Opus 4.7) 判断: P0/P1/P2/P3 全件反映予定

## Codex 所見

### P0
- なし

### P1
1. **plan-id パストラバーサル**: `PLAN_ID_PATTERN` が `/` と `.` を許可し、`detect_active_plan_id()` 後に `_validate_plan_id()` を通していない。`> plan-id: \`../../outside-plan\`` で `.agentops/archive` 外へ task を移動できることを一時 project で確認済み。`PLAN_ID_PATTERN` 自体を狭めるとともに、戻り値も検証すべき。
2. **メタデータ更新と move の順序不整合**: `archive_task` は `next-session.md` 更新後に `move_path` を呼ぶ。移動先で `FileExistsError` などが発生すると task は元に残ったまま `next-session.md` だけ完了扱いになる。`archive_plan` も README row 挿入が move より先で同種の不整合リスク。preflight + move 成功後にメタデータ更新へ寄せ、`OSError` を `AgentOpsError` で包むべき。

### P2
1. **`completed_tasks: []` inline 形式非対応**: `completed_tasks` パースはブロック形式だけ。inline 空配列で CLI は `completed_tasks += 01-demo` と表示する一方、実ファイルには追記されない。docs の「配列に追記」契約として不安定。inline 空配列をブロック形式へ正規化するか、非対応形式なら exit 2 とする。
2. **CLAUDE.md / AGENTS.md と実装・docs の文言ズレ**: durable instructions では「entry_point を空欄に書き換える」と書いたが、実装と docs/11 はマーカー文字列を書く。文言を実装側（マーカー文字列）に揃える。

### P3
1. **docs/11 の `..` 制約と SAFE_PLAN_ID_PATTERN の不一致**: docs/11 は「`..` を含まない前提」と書いたが、`SAFE_PLAN_ID_PATTERN` は `a..b` を許可する。slash 禁止のため traversal にはならないが、仕様と実装を揃えるべき。
2. **`--date` の YYYY-MM-DD 検証と `--summary` の table 文字 escaping 不足**: archive README の table を壊し得るので hardening。

## 確認済み（Codex）

- `python3 -m compileall tools` 成功
- `scripts/agentops archive --help` 成功
- `archive task --dry-run` / `archive plan --dry-run` 成功

## 反映方針と差分

| 指摘 | 対応 | 反映先 |
|---|---|---|
| P1-1 | `PLAN_ID_PATTERN` を `^>?\s*plan-id\s*:\s*\`?([A-Za-z0-9][A-Za-z0-9_.-]*)\`?\s*$` に厳格化。`detect_active_plan_id` 戻り値を `_validate_plan_id` に通す | `tools/agentops_cli/__main__.py` |
| P1-2 | `archive_task` / `archive_plan` を「preflight (dst 衝突確認) → move → メタデータ更新」順に再構成。`move_path` の `OSError` を `AgentOpsError` で包む | 同上 |
| P2-1 | `update_next_session` で `completed_tasks: []` inline 空配列を検出してブロック形式に正規化してから追記 | 同上 |
| P2-2 | CLAUDE.md / AGENTS.md の文言を「entry_point を完了済みマーカー文字列に書き換える」に修正 | `CLAUDE.md`, `AGENTS.md` |
| P3-1 | `SAFE_PLAN_ID_PATTERN` で連続ピリオド (`..`) を弾く（または docs/11 の文言を実装に合わせる）。実装側を厳格化する方向で対応 | `tools/agentops_cli/__main__.py` |
| P3-2 | `--date` を `^\d{4}-\d{2}-\d{2}$` で検証。`--summary` に改行を含むときは exit 2、`|` は table を壊さない escape (`\|`) で書き出す | 同上 |

## 採否判断

- P0/P1/P2/P3 全件反映（DbC・契約の整合に直結する hardening、コスト軽量）。1 周で完了見込み。

## 残タスク

- 反映後に `python3 -m compileall tools` と仮 fixture での動作確認を再実行。
- 反映後の差分が secret を含まないことを確認。
- 反映後に main 同期 + 自身を archive (ドッグフード) で task 07 を archive へ。
